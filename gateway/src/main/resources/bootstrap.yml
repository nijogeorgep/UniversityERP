spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

server:
  port: 8080
  
eureka:
  client:
    service-url:
      defaultZone: http://admin:password@localhost:7000/eureka/